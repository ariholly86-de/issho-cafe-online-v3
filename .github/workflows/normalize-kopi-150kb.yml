name: Normalize Kopi 150KB
on:
  push:
    paths:
      - '.normalize-kopi-trigger'
  workflow_dispatch:
jobs:
  normalize:
    runs-on: ubuntu-latest
    steps:
      - name: Process Kopi images
        shell: bash
        run: |
          set -e
          curl --fail --show-error --silent --max-time 900 "https://xvhimyflrqrdudijwjdn.supabase.co/functions/v1/run-kopi-150kb-white"
          echo
